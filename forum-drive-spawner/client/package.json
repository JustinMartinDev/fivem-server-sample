{
  "name": "@forum-drive-spawner/client",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf ./dist && rm -rf ./node_modules",
    "prebuild": "rm -rf ./dist",
    "--------- BUILD ---------": "--------- BUILD ---------",
    "build": "vite build",
    "--------- WATCH ---------": "--------- WATCH ---------",
    "watch": "vite"
  },
  "devDependencies": {
    "@citizenfx/client": "catalog:",
    "@types/node": "catalog:",
    "@spawnmanager/types": "workspace:*",
    "vite": "catalog:",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=16",
    "pnpm": ">=7"
  }
}
